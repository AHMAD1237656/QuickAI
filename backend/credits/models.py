# Credits are stored directly on the User model (users.User.credits) to
# keep balance lookups simple and always consistent. This app exposes the
# read-only credits API endpoint and, in the future, could hold a ledger
# of purchases/top-ups.
